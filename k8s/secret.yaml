# NOTE: In production, use AWS Secrets Manager or Vault for secrets.
# This file shows the structure only – actual values must be base64-encoded.
# kubectl create secret generic loan-assistance-secrets \
#   --from-literal=POSTGRES_USER=... \
#   --from-literal=POSTGRES_PASSWORD=... \
#   --from-literal=OPENAI_API_KEY=... \
#   --from-literal=JWT_SECRET_KEY=... \
#   -n loan-assistance
#
# Or use External Secrets Operator with AWS Secrets Manager:
# https://external-secrets.io/
apiVersion: v1
kind: Secret
metadata:
  name: loan-assistance-secrets
  namespace: loan-assistance
  labels:
    app.kubernetes.io/name: loan-assistance
    app.kubernetes.io/component: secrets
type: Opaque
# stringData (not committed to git in production):
stringData:
  POSTGRES_USER: "loan_user"
  POSTGRES_PASSWORD: "REPLACE_ME"
  OPENAI_API_KEY: "REPLACE_ME"
  JWT_SECRET_KEY: "REPLACE_ME"
